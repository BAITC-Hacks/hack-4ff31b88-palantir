# Единые переводы строк (LF в репозитории), чтобы Windows и Linux не давали ложных конфликтов
* text=auto
*.parquet binary
*.sqlite binary
# Файл модели хранится байт в байт: его SHA256 записан в журнале агента
model/lightgbm_model.txt -text
